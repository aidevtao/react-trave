import React, { FC } from 'react'
import { UserLayout } from '../../layouts/userLayout';

export const SignInPage: FC = (props) => {
  console.log(props);

  return (
    <UserLayout>
      login
    </UserLayout>
  )
}